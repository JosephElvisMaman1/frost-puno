# Samples

This folder is reserved for explicitly synthetic or demonstration-only datasets.

Current status:

- No separate synthetic weather sample is required because `data/raw/weather_open_meteo.csv` was generated from Open-Meteo Historical API.
- The district seed in `data/external/inei_puno_districts.csv` is documented as an MVP external seed, not as a production-ready official extract.
